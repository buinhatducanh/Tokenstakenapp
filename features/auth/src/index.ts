// Task 1: Auth Feature - Magic Link + WebAuthn (Passkeys)
// Public API surface. Only export what other packages can use.

export { useAuth } from "./api/auth.hooks";
export type { AuthContextValue } from "./api/auth.hooks";
